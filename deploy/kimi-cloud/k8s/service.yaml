apiVersion: v1
kind: Service
metadata:
  name: rtv-kimi-cloud
  labels:
    app: rtv-kimi-cloud
spec:
  type: ClusterIP
  selector:
    app: rtv-kimi-cloud
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
