apiVersion: v1
kind: Secret
metadata:
  name: rtv-kimi-cloud-secrets
  labels:
    entity: darrel-spell-living-trust
type: Opaque
stringData:
  # NEVER commit real values. Create from vault / GitHub secrets / sealed-secrets.
  KIMI_API_KEY: "sk-REPLACE_ME"
  TELEGRAM_BOT_TOKEN_6: "123456789:REPLACE_MAIN_BOT"
  TELEGRAM_BOT_TOKEN_7: "987654321:REPLACE_EROTICA_BOT"
  SUPABASE_SERVICE_ROLE: "REPLACE_ME"
  # Add any other Worker secrets that the Node entry needs when fully wired
